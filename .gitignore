# Private docs — keep locally, never commit
docs/ACCOUNTS.md

# Local venv symlink — not part of the repo
.venv

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.vite

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Reference clones (not tracked)
reference/

# Python virtual environment
.venv/
venv/
env/

# Cursor IDE files
.cursor/
.cursor/debug.log

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# TypeScript build artifacts
*.tsbuildinfo

# Database
*.db
*.sqlite
*.sqlite3
heromaker.db

# Railway environment files with secrets (keep templates, ignore actual keys)
devops/railway/env/backend
devops/railway/env/frontend
devops/railway/env/vrm-converter

# Environment files
.env
.env.railway
*.mov

# Data directory (excluded from git to speed up Railway snapshots)
data/
frontend/dev-dist/

# Worktrees created by Claude Code subagents for isolated runs.
.claude/worktrees/

# Raw marketing photo downloads live in the scratchpad, never the repo.
marketing/photos/*.jpg.orig

# Photos of identifiable people, held back until consent is confirmed.
# This repository publishes to GitHub Pages; committing these would publish them.
marketing/photos/needs-consent/*.jpg

# Anonymised photos: derived from the held-back originals. The product owner
# decides whether any photo of a person is published; anonymising lowers the
# risk but does not establish permission, so these stay out of git too.
marketing/photos/anonymised/*.jpg

# Concept mockup assets are COPIES of files already tracked elsewhere:
# the avatars and clips live in games/hero-moves/assets/, and the drawing crop
# in marketing/photos/. audit.mjs serves them from their real homes, so the
# copies are a local convenience, not a second source of truth.
marketing/concepts/assets/
